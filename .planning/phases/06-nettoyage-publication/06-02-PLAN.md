---
phase: 6
plan: "06-02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - "(historique git réécrit — pas de fichiers sources modifiés)"
autonomous: false
requirements:
  - PUB-03
  - PUB-04
---

<objective>
Réécrire l'historique Git en un seul commit "Initial release" sur la branche `main`, avec l'auteur Tuture57. Remplace tous les commits précédents par un historique vierge propre.

**ATTENTION — action irréversible.** L'exécuteur doit confirmer avec l'utilisateur avant de lancer les commandes git destructives (étapes 2-4).
</objective>

<tasks>

<task id="T1">
<title>Vérifier l'état du repo avant réécriture</title>
<type>execute</type>
<read_first>
- git log --oneline (voir l'historique actuel)
- git status (s'assurer que tout est propre ou stagé)
- git branch (voir la branche actuelle)
</read_first>
<action>
Vérifier les préconditions avant la réécriture :

```
git log --oneline
git status
git branch
git config user.name
git config user.email
```

Confirmer :
- La branche active est `master`
- `git status` montre working tree clean (ou uniquement les fichiers staged du plan 06-01 non encore commités — c'est OK, l'orphan branch va les inclure via `git add -A`)
- `git config user.name` retourne `Tuture57`

Si `git config user.name` ne retourne pas `Tuture57`, afficher un avertissement mais continuer (l'utilisateur peut corriger sa config globale si besoin).
</action>
<acceptance_criteria>
- `git branch` montre `master` comme branche active (préfixé `*`)
- `git config user.name` retourne une valeur non vide
- La commande `git log --oneline` s'exécute sans erreur
</acceptance_criteria>
</task>

<task id="T2">
<title>Créer branche orpheline et préparer le commit unique</title>
<type>execute</type>
<read_first>
- (résultat de T1 — préconditions validées)
</read_first>
<action>
**CHECKPOINT — confirmer avec l'utilisateur avant d'exécuter ces commandes.**

Créer une branche orpheline `fresh-start`, y ajouter tous les fichiers, et créer le commit unique :

```bash
git checkout --orphan fresh-start
git add -A
git commit -m "Initial release — AutoFarm Dungeon Bot v1.0"
```

Vérifier après le commit :
```bash
git log --oneline
```
Doit afficher exactement 1 ligne avec le message "Initial release — AutoFarm Dungeon Bot v1.0".
</action>
<acceptance_criteria>
- `git branch` montre `fresh-start` comme branche active
- `git log --oneline | wc -l` retourne `1`
- `git log --oneline` contient `Initial release — AutoFarm Dungeon Bot v1.0`
- `git show --stat HEAD` ne montre pas bot_backup.py, test_clicks.py, test_admin_background.py
- `git show --stat HEAD` montre config.json.example dans les fichiers ajoutés
- `git show --stat HEAD` ne montre pas config.json dans les fichiers ajoutés
</acceptance_criteria>
</task>

<task id="T3">
<title>Remplacer master et renommer en main</title>
<type>execute</type>
<read_first>
- (résultat de T2 — commit unique validé sur fresh-start)
</read_first>
<action>
Supprimer l'ancienne branche `master` et renommer `fresh-start` en `main` :

```bash
git branch -D master
git branch -m fresh-start main
```

Vérifier :
```bash
git branch
git log --oneline
```
</action>
<acceptance_criteria>
- `git branch` montre uniquement `main` (plus de `master`, plus de `fresh-start`)
- `git log --oneline` montre toujours exactement 1 commit
- `git log --format="%an"` retourne le nom d'auteur configuré (Tuture57 ou valeur de git config user.name)
- `git show --stat HEAD` montre les fichiers du repo nettoyé (README.md, bot.py, main.py, config.py, capture_templates.py, requirements.txt, templates/*, docs/*, .gitignore, config.json.example)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `git log --oneline` — exactement 1 ligne, message "Initial release — AutoFarm Dungeon Bot v1.0"
2. `git branch` — uniquement `main`
3. `git show --stat HEAD` — aucun fichier test/debug, config.json absent, config.json.example présent
4. `git log --format="%an %ae"` — auteur = identité git locale
5. `ls` à la racine — bot_backup.py, test_clicks.py, test_admin_background.py absents
</verification>

<success_criteria>
- [ ] Historique Git = exactement 1 commit sur branche main
- [ ] Message du commit = "Initial release — AutoFarm Dungeon Bot v1.0"
- [ ] Auteur du commit = identité git globale (Tuture57)
- [ ] La branche `master` n'existe plus
- [ ] config.json absent du commit (ignoré par .gitignore)
- [ ] config.json.example présent dans le commit
- [ ] Aucun fichier test/debug dans le commit
</success_criteria>

<must_haves>
- Historique Git vierge — 1 seul commit (PUB-03)
- Auteur = Tuture (PUB-04)
- Branche finale = main (D-12)
- config.json absent du commit (D-03)
</must_haves>
