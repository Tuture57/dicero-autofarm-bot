---
phase: 6
plan: "06-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - config.json.example
  - "!bot_backup.py"
  - "!test_clicks.py"
  - "!test_admin_background.py"
autonomous: true
requirements:
  - PUB-01
  - PUB-02
---

<objective>
Préparer le repo pour publication : supprimer les fichiers de développement, créer `.gitignore`, et créer `config.json.example` avec les valeurs calibrées actuelles. Aucun code Python modifié.
</objective>

<tasks>

<task id="T1">
<title>Supprimer les fichiers de développement</title>
<type>execute</type>
<read_first>
- bot_backup.py (confirmer que c'est une sauvegarde obsolète, pas le bot actif)
- test_clicks.py (confirmer que c'est un script de dev)
- bot.py (confirmer que c'est le fichier actif, différent de bot_backup.py)
</read_first>
<action>
Supprimer les trois fichiers suivants du repo et du disque :
1. `bot_backup.py` — sauvegarde obsolète
2. `test_clicks.py` — script de test de développement
3. `test_admin_background.py` — script de test non versionné

Commandes :
```
git rm bot_backup.py
git rm test_clicks.py
rm test_admin_background.py
```

Note : `test_admin_background.py` n'est pas tracké par git (untracked), donc `rm` suffit, pas `git rm`.
</action>
<acceptance_criteria>
- `ls bot_backup.py` retourne "No such file or directory"
- `ls test_clicks.py` retourne "No such file or directory"
- `ls test_admin_background.py` retourne "No such file or directory"
- `git status` ne montre plus `test_admin_background.py` dans les fichiers untracked
- `git status` montre `bot_backup.py` et `test_clicks.py` en `deleted`
</acceptance_criteria>
</task>

<task id="T2">
<title>Créer .gitignore</title>
<type>execute</type>
<read_first>
- (vérifier si .gitignore existe déjà dans le repo racine)
</read_first>
<action>
Créer le fichier `.gitignore` à la racine du repo avec exactement ce contenu :

```
__pycache__/
*.pyc
*.pyo
config.json
.venv/
venv/
```

Si `.gitignore` existe déjà, remplacer son contenu par les lignes ci-dessus.
Ensuite : `git add .gitignore`
</action>
<acceptance_criteria>
- `.gitignore` existe à la racine du repo
- `cat .gitignore` contient la ligne `__pycache__/`
- `cat .gitignore` contient la ligne `config.json`
- `cat .gitignore` contient la ligne `.venv/`
- `git check-ignore config.json` retourne `config.json` (confirmant que config.json est ignoré)
- `git status` montre `.gitignore` comme new file ou modified
</acceptance_criteria>
</task>

<task id="T3">
<title>Créer config.json.example</title>
<type>execute</type>
<read_first>
- config.json (copier les valeurs exactes)
- config.py (vérifier le nom du fichier attendu au runtime)
</read_first>
<action>
Créer `config.json.example` avec exactement le contenu suivant (valeurs calibrées actuelles de `config.json`) :

```json
{
    "mirrorto_title": "MirrorTo",
    "templates_dir": "templates",
    "game_left_offset": 80,
    "game_top_offset": 79,
    "game_right_offset": 19,
    "game_bottom_offset": 80,
    "confidence": 0.75,
    "loop_delay": 0.5,
    "click_delay": 0.3,
    "wheel_wait": 3.0,
    "card_positions": [
        [205, 290],
        [206, 380],
        [207, 474]
    ],
    "dice_position": [72, 594]
}
```

Ensuite : `git add config.json.example`
</action>
<acceptance_criteria>
- `config.json.example` existe à la racine du repo
- `grep "mirrorto_title" config.json.example` retourne `"mirrorto_title": "MirrorTo"`
- `grep "game_left_offset" config.json.example` retourne `"game_left_offset": 80`
- `grep "confidence" config.json.example` retourne `"confidence": 0.75`
- `git status` montre `config.json.example` comme new file
- `git check-ignore config.json.example` ne retourne rien (le fichier n'est PAS ignoré)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `git status` montre : deleted bot_backup.py, deleted test_clicks.py, new file .gitignore, new file config.json.example
2. `git check-ignore config.json` retourne `config.json`
3. `ls bot_backup.py test_clicks.py test_admin_background.py` — tous absents
4. `cat config.json.example` est JSON valide avec les bonnes clés
</verification>

<success_criteria>
- [ ] bot_backup.py supprimé (git rm)
- [ ] test_clicks.py supprimé (git rm)
- [ ] test_admin_background.py supprimé (rm, untracked)
- [ ] .gitignore créé avec les 6 entrées requises (inclus config.json)
- [ ] config.json.example créé avec les valeurs calibrées actuelles
- [ ] git status cohérent avec les changements attendus
</success_criteria>

<must_haves>
- config.json est ignoré par .gitignore (PUB-02)
- Aucun fichier test/debug reste dans le working directory (PUB-01)
- config.json.example est versionné et contient les vraies valeurs (D-01, D-02)
</must_haves>
