---
id: "03-02"
wave: 2
title: "Créer main.py avec menu rich + questionary"
goal: "Créer le point d'entrée unique main.py avec auto-élévation admin, titre ASCII art, menu flèches questionary, retour auto au menu"
requirements_addressed: [UX-01, UX-02, UX-03, UX-04]
files_modified:
  - main.py
  - requirements.txt
depends_on: "03-01"
autonomous: true
---

<objective>
Créer main.py comme point d'entrée unique de l'application. Le fichier contient dans cet ordre strict :
1. L'auto-élévation admin (PREMIER bloc exécutable, avant tout import win32)
2. Les imports de bot et capture_templates
3. La boucle de menu rich + questionary avec retour automatique après chaque action

Ajouter `rich` et `questionary` à requirements.txt.
</objective>

<tasks>
<task id="T1">
<title>Ajouter rich et questionary à requirements.txt</title>
<read_first>
- requirements.txt (confirmer le contenu actuel avant d'éditer)
</read_first>
<action>
requirements.txt contient actuellement :
```
pyautogui
opencv-python
Pillow
numpy
pywin32
```

Ajouter deux lignes à la fin :
```
rich
questionary
```

SEARCH/REPLACE :
```
<<<<<<< SEARCH
pyautogui
opencv-python
Pillow
numpy
pywin32
=======
pyautogui
opencv-python
Pillow
numpy
pywin32
rich
questionary
>>>>>>> REPLACE
```
</action>
<acceptance_criteria>
- requirements.txt contient `rich` sur une ligne séparée
- requirements.txt contient `questionary` sur une ligne séparée
- Les 5 lignes d'origine sont toujours présentes et inchangées
</acceptance_criteria>
</task>

<task id="T2">
<title>Créer main.py avec élévation admin, menu rich + questionary</title>
<read_first>
- main.py (vérifier que le fichier n'existe pas encore — si absent, la lecture retournera une erreur, ce qui confirme qu'on peut le créer)
</read_first>
<action>
Créer le fichier main.py avec le contenu exact suivant. L'ordre des sections est CRITIQUE :

```python
import ctypes
import sys

if not ctypes.windll.shell32.IsUserAnAdmin():
    ctypes.windll.shell32.ShellExecuteW(
        None, "runas", sys.executable,
        " ".join(f'"{a}"' for a in sys.argv), None, 1
    )
    sys.exit(0)

import bot
import capture_templates
import questionary
from rich.console import Console
from rich.panel import Panel

console = Console()

ASCII_ART = r"""
   ___         __        ____
  / _ | __ __ / /_ ___  / __/___ _______ _
 / __ |/ // // __// _ \/ _/ / _ `/ __/  ' \
/_/ |_|\_,_/ \__/ \___/_/   \_,_/_/ /_/_/_/
"""


def show_header():
    console.print(Panel(
        f"[bold cyan]{ASCII_ART}[/bold cyan]",
        subtitle="[dim]Ctrl+C pour quitter[/dim]",
        expand=False,
        border_style="cyan",
    ))


def run_menu():
    while True:
        console.clear()
        show_header()

        choice = questionary.select(
            "Que veux-tu faire ?",
            choices=[
                questionary.Choice("Calibrer les templates", value="calibrate"),
                questionary.Choice("Lancer le bot", value="run"),
                questionary.Separator(),
                questionary.Choice("Quitter", value="quit"),
            ],
            pointer="»",
        ).ask()

        if choice is None or choice == "quit":
            console.print("[dim]À bientôt.[/dim]")
            break

        if choice == "run":
            console.print("[bold green]Lancement du bot...[/bold green]")
            try:
                bot.main()
            except KeyboardInterrupt:
                pass
            console.print("[yellow]Bot arrêté. Retour au menu...[/yellow]")
            questionary.press_any_key_to_continue(
                "Appuie sur une touche pour revenir au menu."
            ).ask()

        elif choice == "calibrate":
            console.print("[bold blue]Lancement de la calibration...[/bold blue]")
            try:
                capture_templates.main()
            except KeyboardInterrupt:
                pass
            console.print("[yellow]Calibration terminée. Retour au menu...[/yellow]")
            questionary.press_any_key_to_continue(
                "Appuie sur une touche pour revenir au menu."
            ).ask()


if __name__ == "__main__":
    run_menu()
```

Points clés à respecter lors de la création :
- `import ctypes` et `import sys` sont les DEUX PREMIERS imports (avant tout autre)
- Le bloc `if not ctypes.windll.shell32.IsUserAnAdmin():` vient juste après, avant `import bot`
- `import bot` et `import capture_templates` arrivent APRÈS le bloc élévation
- `console.clear()` est appelé à chaque itération pour rafraîchir l'affichage
- `bot.main()` est wrappé dans `try/except KeyboardInterrupt: pass` (filet de sécurité)
- `capture_templates.main()` est wrappé dans `try/except KeyboardInterrupt: pass` (nécessaire — input() propage KeyboardInterrupt)
- `questionary.press_any_key_to_continue(...)` permet de lire le résumé avant le clear
</action>
<acceptance_criteria>
- main.py existe dans C:\Users\barto\Desktop\autofarm\
- main.py contient `import ctypes` et `import sys` avant tout autre import
- main.py contient `IsUserAnAdmin` et `ShellExecuteW` dans le bloc d'élévation
- main.py contient `import bot` et `import capture_templates` APRÈS le bloc élévation
- main.py contient `from rich.console import Console`
- main.py contient `import questionary`
- main.py contient `def show_header(`
- main.py contient `def run_menu(`
- main.py contient `while True:`
- main.py contient `if choice is None or choice == "quit":`
- main.py contient `bot.main()`
- main.py contient `capture_templates.main()`
- main.py contient `if __name__ == "__main__":` suivi de `run_menu()`
- `grep -n "ShellExecuteW" main.py` retourne 1 résultat (dans le bloc élévation)
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `python main.py` depuis le terminal → doit afficher le menu rich avec titre ASCII et 3 options (Calibrer / Lancer le bot / Quitter)
2. Naviguer avec les flèches haut/bas → le curseur `»` se déplace correctement
3. Sélectionner "Quitter" → le programme se ferme proprement avec "À bientôt."
4. Ctrl+C depuis le menu (choice is None) → le programme se ferme sans stack trace
5. Sélectionner "Calibrer les templates" → capture_templates.main() se lance, puis le menu réapparaît
6. Sélectionner "Lancer le bot" → bot.main() se lance, Echap arrête le bot, le menu réapparaît
7. `grep -n "ShellExecuteW" bot.py capture_templates.py` → aucun résultat (confirme 03-01 appliqué)
8. `grep -n "ShellExecuteW" main.py` → 1 résultat
9. `grep -n "rich\|questionary" requirements.txt` → 2 résultats
</verification>

<must_haves>
- `python main.py` affiche un menu stylisé avec titre ASCII (UX-01)
- L'option "Calibrer les templates" lance capture_templates.main() (UX-02)
- L'option "Lancer le bot" lance bot.main() (UX-03)
- Ctrl+C depuis le menu quitte sans stack trace ; Ctrl+C pendant le bot ou la calibration retourne au menu (UX-04)
- Le menu réapparaît automatiquement après chaque action (D-03)
- L'élévation admin est le premier bloc exécutable de main.py (D-02)
- rich et questionary dans requirements.txt (D-08)
</must_haves>
