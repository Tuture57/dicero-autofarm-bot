---
id: "03-01"
wave: 1
title: "Refactor bot.py and capture_templates.py — remove elevation, make importable"
goal: "Retirer le bloc auto-élévation admin des deux modules pour qu'ils soient importables sans side-effects"
requirements_addressed: []
files_modified:
  - bot.py
  - capture_templates.py
autonomous: true
---

<objective>
Rendre bot.py et capture_templates.py importables sans déclencher d'élévation admin au moment de l'import. Le bloc ShellExecuteW/sys.exit sera retiré des deux fichiers. sys sera aussi retiré de capture_templates.py si devenu inutilisé. Après ce plan, main.py (plan 03-02) peut importer ces modules librement.
</objective>

<tasks>
<task id="T1">
<title>Retirer le bloc auto-élévation de bot.py</title>
<read_first>
- bot.py (lire pour confirmer le texte exact des lignes 21-23 avant tout edit)
</read_first>
<action>
Dans bot.py, supprimer exactement ces 3 lignes (lignes 21-23 actuelles) :

```
if not ctypes.windll.shell32.IsUserAnAdmin():
    ctypes.windll.shell32.ShellExecuteW(None, "runas", sys.executable, " ".join(f'"{a}"' for a in sys.argv), None, 1)
    sys.exit(0)
```

Le résultat attendu : après la ligne `log = logging.getLogger(__name__)` (ligne 19), la prochaine ligne est directement `cfg = config.load()`.

L'import `sys` à la ligne 6 doit RESTER dans bot.py car `sys` n'est pas utilisé ailleurs dans ce fichier — vérifier avant de toucher quoi que ce soit. (Si une inspection montre que sys n'est utilisé nulle part ailleurs, le retirer aussi — mais ne pas retirer sans vérifier.)

SEARCH/REPLACE exact :
```
<<<<<<< SEARCH
if not ctypes.windll.shell32.IsUserAnAdmin():
    ctypes.windll.shell32.ShellExecuteW(None, "runas", sys.executable, " ".join(f'"{a}"' for a in sys.argv), None, 1)
    sys.exit(0)

cfg = config.load()
=======
cfg = config.load()
>>>>>>> REPLACE
```
</action>
<acceptance_criteria>
- bot.py ne contient plus `ShellExecuteW`
- bot.py ne contient plus `IsUserAnAdmin`
- bot.py contient toujours `import sys` (ou sys n'est effectivement pas utilisé et a été retiré proprement)
- bot.py contient toujours `def main(` et `if __name__ == "__main__":`
- `grep -n "ShellExecuteW" bot.py` retourne aucun résultat
</acceptance_criteria>
</task>

<task id="T2">
<title>Retirer le bloc auto-élévation de capture_templates.py et nettoyer sys</title>
<read_first>
- capture_templates.py (lire pour confirmer le texte exact des lignes 9, 17-19 avant tout edit)
</read_first>
<action>
Dans capture_templates.py :

ÉTAPE 1 — Retirer le bloc élévation (lignes 17-19 actuelles) :
```
if not ctypes.windll.shell32.IsUserAnAdmin():
    ctypes.windll.shell32.ShellExecuteW(None, "runas", sys.executable, " ".join(f'"{a}"' for a in sys.argv), None, 1)
    sys.exit(0)
```

ÉTAPE 2 — Retirer aussi `import sys` (ligne 9 actuelle) car sys n'est utilisé QUE dans ces 3 lignes (vérification : grep sys dans capture_templates.py confirme 0 autre usage).

SEARCH/REPLACE pour l'élévation + ligne vide suivante :
```
<<<<<<< SEARCH
if not ctypes.windll.shell32.IsUserAnAdmin():
    ctypes.windll.shell32.ShellExecuteW(None, "runas", sys.executable, " ".join(f'"{a}"' for a in sys.argv), None, 1)
    sys.exit(0)

cfg = config.load()
=======
cfg = config.load()
>>>>>>> REPLACE
```

SEARCH/REPLACE pour retirer `import sys` :
```
<<<<<<< SEARCH
import sys
import win32gui
=======
import win32gui
>>>>>>> REPLACE
```

Résultat final attendu en haut du fichier (après docstring) :
```python
import cv2
import numpy as np
import os
import win32gui
import win32ui
import ctypes
import config

ctypes.windll.shcore.SetProcessDpiAwareness(2)

cfg = config.load()
```
</action>
<acceptance_criteria>
- capture_templates.py ne contient plus `ShellExecuteW`
- capture_templates.py ne contient plus `IsUserAnAdmin`
- capture_templates.py ne contient plus `import sys`
- capture_templates.py contient toujours `def main(` et `if __name__ == "__main__":`
- capture_templates.py contient toujours `ctypes.windll.shcore.SetProcessDpiAwareness(2)` (ligne top-level)
- `grep -n "ShellExecuteW" capture_templates.py` retourne aucun résultat
- `grep -n "import sys" capture_templates.py` retourne aucun résultat
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `grep -rn "ShellExecuteW" bot.py capture_templates.py` → aucun résultat
2. `grep -n "import sys" capture_templates.py` → aucun résultat
3. `python -c "import bot"` → doit s'exécuter sans déclencher de popup UAC ni sys.exit (peut échouer sur win32 sans fenêtre MirrorTo ouverte, mais ne doit PAS déclencher d'élévation)
4. `python -c "import capture_templates"` → idem
5. Lecture manuelle de bot.py lignes 1-30 et capture_templates.py lignes 1-25 pour confirmer la structure correcte
</verification>

<must_haves>
- Aucune occurrence de ShellExecuteW dans bot.py ou capture_templates.py
- bot.py et capture_templates.py restent fonctionnels en standalone (`python bot.py`, `python capture_templates.py`) — la vérification admin sera désormais dans main.py uniquement
- `import sys` retiré de capture_templates.py (il n'y est plus utilisé)
- Aucune autre ligne modifiée dans les deux fichiers
</must_haves>
