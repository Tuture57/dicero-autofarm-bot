---
phase: 5
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/screenshot.png
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
---

<objective>
Replace README.md with a complete English README for GitHub publication. Create the docs/ directory with a screenshot placeholder. The README covers prerequisites, numbered installation steps, usage via `python main.py`, recalibration, and credits Tuture as author. No Python files are modified.
</objective>

<tasks>

<task id="1" type="execute">
  <name>Create docs/ directory with screenshot placeholder</name>
  <read_first>
    - .planning/phases/05-readme/05-CONTEXT.md (D-09: placeholder at docs/screenshot.png)
  </read_first>
  <action>
Create the file `docs/screenshot.png` as an empty placeholder. This file will be replaced manually by the author before GitHub publication. Just ensure the path exists so the README img tag does not produce a broken-image error in the repo.

Create an empty binary file at `docs/screenshot.png`. A 1×1 transparent PNG is acceptable (any minimal valid PNG bytes).
  </action>
  <acceptance_criteria>
    - `docs/screenshot.png` exists in the repo
    - File size > 0 bytes
  </acceptance_criteria>
</task>

<task id="2" type="execute">
  <name>Write complete README.md</name>
  <read_first>
    - README.md (current content — to be replaced entirely)
    - requirements.txt (7 dependencies: pyautogui, opencv-python, Pillow, numpy, pywin32, rich, questionary)
    - main.py (menu options: "Calibrer les templates", "Lancer le bot", "Quitter"; auto-admin elevation via ShellExecuteW)
    - .planning/phases/05-readme/05-CONTEXT.md (all decisions D-01 through D-11)
    - .planning/REQUIREMENTS.md (DOC-01 through DOC-05)
  </read_first>
  <action>
Replace README.md entirely with the following content (copy verbatim):

```markdown
# AutoFarm — Dungeon Bot

![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-blue)

An automation bot for a dungeon mobile game, mirrored to PC via MirrorTo. Handles all game states autonomously — attack loop, skill selection, dice selection, fortune wheel, and confirmation popups.

![Menu screenshot](docs/screenshot.png)

## Prerequisites

- **Windows 11** (required — uses Windows-only APIs)
- **Python 3.10+**
- **MirrorTo** — Android mirroring app running with the game visible at a fixed window size
- **Administrator rights** — the app auto-elevates on launch (a UAC prompt will appear)

## Installation

1. Clone or download this repository.
2. Install dependencies:
   ```
   pip install -r requirements.txt
   ```
3. Start the bot:
   ```
   python main.py
   ```

## Usage

Running `python main.py` opens an interactive menu with three options:

- **Calibrate templates** — capture reference images for scene detection (run once, or after resizing/moving the MirrorTo window)
- **Run bot** — start the automation loop
- **Quit** — exit the program

Press `Ctrl+C` at any time to stop and return to the menu.

## Recalibration

If the bot stops detecting game states correctly after moving or resizing the MirrorTo window, select **Calibrate templates** from the menu and follow the on-screen prompts to re-capture reference images.

## Author

**Tuture**
```

Exact requirements mapping:
- DOC-01: Prerequisites section lists Python, MirrorTo, pip
- DOC-02: Installation section has numbered steps, copy-paste ready
- DOC-03: Usage section explains `python main.py` and menu options
- DOC-04: Recalibration section explains the Calibrate option
- DOC-05: Author section at bottom credits Tuture
  </action>
  <acceptance_criteria>
    - `README.md` contains `## Prerequisites`
    - `README.md` contains `## Installation`
    - `README.md` contains `pip install -r requirements.txt`
    - `README.md` contains `python main.py`
    - `README.md` contains `## Usage`
    - `README.md` contains `## Recalibration`
    - `README.md` contains `## Author`
    - `README.md` contains `Tuture`
    - `README.md` contains `shields.io`
    - `README.md` does NOT contain `capture_templates.py` (old workflow removed)
    - `README.md` does NOT contain `bot.py` (old workflow removed)
    - `README.md` does NOT contain French section headers (all headers in English)
    - `README.md` contains `docs/screenshot.png`
    - `README.md` contains `Windows 11`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `README.md` exists at repo root and contains all required sections (Prerequisites, Installation, Usage, Recalibration, Author)
2. `README.md` is entirely in English (section headers and body text)
3. `docs/screenshot.png` exists as a placeholder
4. Old workflow references (`capture_templates.py`, `bot.py` as standalone commands) are absent
5. Author "Tuture" is visible at bottom of README
6. shields.io Python 3.10+ badge is present
7. No behavior table (state→action) present (D-10 exclusion respected)
8. No troubleshooting section (D-11 exclusion respected)
</verification>

<success_criteria>
- DOC-01 ✓: Prerequisites section lists Python 3.10+, Windows 11, MirrorTo, pip
- DOC-02 ✓: Numbered installation steps, copy-paste ready commands
- DOC-03 ✓: Usage section explains `python main.py` and menu options
- DOC-04 ✓: Recalibration section explains when and how to recalibrate
- DOC-05 ✓: Author = Tuture, visible at bottom
- All 5 DOC requirements addressed in a single plan
</success_criteria>

<must_haves>
- README.md fully replaced (not appended)
- All 5 DOC-0x requirements covered
- English throughout
- Tuture credited as author
- docs/screenshot.png placeholder exists
</must_haves>
