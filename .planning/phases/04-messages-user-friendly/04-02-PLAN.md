---
id: "04-02"
wave: 2
title: "Refactor capture_templates.py — questionary menu + rich Console + Panel instructions"
goal: "Remplacer le menu print()/input() par questionary.select(), les messages de statut par rich Console, les instructions de calibration par rich Panel"
requirements_addressed: [UX-06, UX-07]
files_modified:
  - capture_templates.py
depends_on: "04-01"
autonomous: true
---

<objective>
Moderniser l'interface utilisateur de capture_templates.py sans toucher à la logique de capture OpenCV, de sauvegarde des templates ou de calibration des positions. Trois changements :
1. Remplacer le menu `print()`/`input()` par `questionary.select()` — navigation flèches
2. Remplacer les `print()` de statut par `rich Console.print()` avec couleurs
3. Encadrer les instructions de calibration dans des `rich Panel` avant chaque étape
</objective>

<tasks>
<task id="T1">
<title>Ajouter les imports rich et questionary + initialiser console</title>
<read_first>
- capture_templates.py (lire lignes 1-25 pour voir les imports actuels)
- main.py (lire lignes 1-20 pour voir le pattern d'import rich + questionary utilisé en Phase 3)
</read_first>
<action>
Ajouter les imports après `import config` (ligne ~12) et initialiser `console` :

SEARCH/REPLACE :
```
<<<<<<< SEARCH
import config

ctypes.windll.shcore.SetProcessDpiAwareness(2)
=======
import config
import questionary
from rich.console import Console
from rich.panel import Panel

console = Console()

ctypes.windll.shcore.SetProcessDpiAwareness(2)
>>>>>>> REPLACE
```

Résultat attendu en haut du fichier (après docstring) :
```python
import cv2
import numpy as np
import os
import win32gui
import win32ui
import ctypes
import config
import questionary
from rich.console import Console
from rich.panel import Panel

console = Console()

ctypes.windll.shcore.SetProcessDpiAwareness(2)
```
</action>
<acceptance_criteria>
- `grep -n "import questionary" capture_templates.py` retourne 1 résultat
- `grep -n "from rich.console" capture_templates.py` retourne 1 résultat
- `grep -n "from rich.panel" capture_templates.py` retourne 1 résultat
- `grep -n "console = Console()" capture_templates.py` retourne 1 résultat
</acceptance_criteria>
</task>

<task id="T2">
<title>Remplacer les print() de statut et messages par rich Console.print()</title>
<read_first>
- capture_templates.py (lire lignes 72-165 pour voir les print() dans calibrate_offset, _calibrate_clicks, calibrate_cards, calibrate_dice, show_and_select)
</read_first>
<action>
Remplacer les messages de statut dans les fonctions de calibration :

calibrate_offset — messages de résultat (lignes ~86-89, ~97) :
```
<<<<<<< SEARCH
        print(f"\n  game_left_offset   = {offsets[0]}")
        print(f"  game_top_offset    = {offsets[1]}")
        print(f"  game_right_offset  = {offsets[2]}")
        print(f"  game_bottom_offset = {offsets[3]}")
=======
        console.print(f"\n  game_left_offset   = [cyan]{offsets[0]}[/cyan]")
        console.print(f"  game_top_offset    = [cyan]{offsets[1]}[/cyan]")
        console.print(f"  game_right_offset  = [cyan]{offsets[2]}[/cyan]")
        console.print(f"  game_bottom_offset = [cyan]{offsets[3]}[/cyan]")
>>>>>>> REPLACE
```

_calibrate_clicks — clic enregistré (ligne ~110) :
```
<<<<<<< SEARCH
                print(f"  {labels[idx]} enregistre : ({x}, {y})")
=======
                console.print(f"  [green]{labels[idx]}[/green] enregistré : ({x}, {y})")
>>>>>>> REPLACE
```

_calibrate_clicks — capture vide (ligne ~97) :
```
<<<<<<< SEARCH
        print("Erreur: capture vide. Verifie que MirrorTo est ouvert.")
=======
        console.print("[red]Erreur : capture vide. Vérifie que MirrorTo est ouvert.[/red]")
>>>>>>> REPLACE
```

calibrate_cards — résultat (lignes ~132, ~136-137) :
```
<<<<<<< SEARCH
    if clicks is None:
        print("Calibration incomplete. Annule.")
        return
    cfg["card_positions"] = clicks
    config.save(cfg)
    print(f"\ncard_positions = {clicks}")
    print("Positions sauvegardees dans config.json.")
=======
    if clicks is None:
        console.print("[yellow]Calibration incomplète. Annulé.[/yellow]")
        return
    cfg["card_positions"] = clicks
    config.save(cfg)
    console.print(f"\n[dim]card_positions = {clicks}[/dim]")
    console.print("[green]Positions des cartes sauvegardées.[/green]")
>>>>>>> REPLACE
```

calibrate_dice — résultat (lignes ~146, ~150-151) :
```
<<<<<<< SEARCH
    if clicks is None:
        print("Calibration incomplete. Annule.")
        return
    cfg["dice_position"] = clicks[0]
    config.save(cfg)
    print(f"\ndice_position = {clicks[0]}")
    print("Position sauvegardee dans config.json.")
=======
    if clicks is None:
        console.print("[yellow]Calibration incomplète. Annulé.[/yellow]")
        return
    cfg["dice_position"] = clicks[0]
    config.save(cfg)
    console.print(f"\n[dim]dice_position = {clicks[0]}[/dim]")
    console.print("[green]Position du dé sauvegardée.[/green]")
>>>>>>> REPLACE
```

show_and_select — template sauvegardé (ligne ~163) :
```
<<<<<<< SEARCH
        print(f"Saved: {path} ({w}x{h})")
=======
        console.print(f"[green]Template sauvegardé :[/green] {path} ({w}×{h})")
>>>>>>> REPLACE
```
</action>
<acceptance_criteria>
- `grep -n "console.print" capture_templates.py` retourne au moins 8 résultats
- `grep -n "print(\"Erreur" capture_templates.py` retourne 0 résultats (remplacés par console.print)
- `grep -n "print(\"Calibration incomplete" capture_templates.py` retourne 0 résultats
- `grep -n "print(\"Positions sauvegardees" capture_templates.py` retourne 0 résultats
- `grep -n "print(f\"Saved:" capture_templates.py` retourne 0 résultats
</acceptance_criteria>
</task>

<task id="T3">
<title>Remplacer les instructions de calibration par rich Panel</title>
<read_first>
- capture_templates.py (lire lignes 72-95 et 126-143 pour voir les print() d'instructions dans calibrate_offset, calibrate_cards, calibrate_dice)
</read_first>
<action>
Encadrer les instructions de calibration dans des rich Panel :

calibrate_offset — instructions (lignes ~76-78) :
```
<<<<<<< SEARCH
    print("\n=== CALIBRATION ===")
    print("Dessine un rectangle autour de la zone du jeu uniquement (sans les barres MirrorTo).")
    print("Espace/Entree pour confirmer, C pour annuler.\n")
=======
    console.print(Panel(
        "Dessine un rectangle autour de la zone du jeu uniquement (sans les barres MirrorTo).\n"
        "[dim]Espace/Entrée pour confirmer, C pour annuler.[/dim]",
        title="[bold]Calibration des offsets[/bold]",
        border_style="cyan",
    ))
>>>>>>> REPLACE
```

calibrate_cards — instructions (lignes ~127-129) :
```
<<<<<<< SEARCH
    print("\n=== CALIBRATION CARTES DE SKILL ===")
    print("Navigue vers l'ecran Select Skill, puis clique sur chaque carte (1, 2, 3).")
    print("Appuie sur Q ou Echap quand tu as fini.\n")
=======
    console.print(Panel(
        "Navigue vers l'écran [bold]Select Skill[/bold], puis clique sur chaque carte (1, 2, 3).\n"
        "[dim]Appuie sur Q ou Echap quand tu as fini.[/dim]",
        title="[bold]Calibration cartes de skill[/bold]",
        border_style="cyan",
    ))
>>>>>>> REPLACE
```

calibrate_dice — instructions (lignes ~141-143) :
```
<<<<<<< SEARCH
    print("\n=== CALIBRATION DE ===")
    print("Navigue vers l'ecran Select Dice, puis clique sur le premier de (gauche) de la rangee du haut.")
    print("Appuie sur Q ou Echap quand tu as fini.\n")
=======
    console.print(Panel(
        "Navigue vers l'écran [bold]Select Dice[/bold], puis clique sur le premier dé (gauche) de la rangée du haut.\n"
        "[dim]Appuie sur Q ou Echap quand tu as fini.[/dim]",
        title="[bold]Calibration position du dé[/bold]",
        border_style="cyan",
    ))
>>>>>>> REPLACE
```
</action>
<acceptance_criteria>
- `grep -n "Panel(" capture_templates.py` retourne 3 résultats (offset + cards + dice)
- `grep -n "print(\"\\\\n=== CALIBRATION" capture_templates.py` retourne 0 résultats (ancien print supprimé)
- `grep -n "border_style=\"cyan\"" capture_templates.py` retourne 3 résultats
</acceptance_criteria>
</task>

<task id="T4">
<title>Remplacer le menu principal print()/input() par questionary.select()</title>
<read_first>
- capture_templates.py (lire lignes 183-273 pour voir la fonction main() entière)
- main.py (lire lignes 40-80 pour voir le pattern questionary.select() utilisé en Phase 3)
</read_first>
<action>
Remplacer tout le menu principal dans `main()`. La structure de la boucle while reste identique, seule la présentation change.

ÉTAPE 1 — Remplacer l'affichage de l'en-tête de la fenêtre MirrorTo trouvée :
```
<<<<<<< SEARCH
    if not hwnd:
        print("Erreur: fenetre MirrorTo introuvable. Lance MirrorTo d'abord.")
        return
    print(f"Fenetre MirrorTo trouvee (hwnd={hwnd})")
=======
    if not hwnd:
        console.print("[red]Fenêtre MirrorTo introuvable. Lance MirrorTo, puis réessaie.[/red]")
        return
    console.print(f"[dim]Fenêtre MirrorTo trouvée (hwnd={hwnd})[/dim]")
>>>>>>> REPLACE
```

ÉTAPE 2 — Remplacer le message de sauvegarde des offsets :
```
<<<<<<< SEARCH
        print("Offsets sauvegardes dans config.json.\n")
=======
        console.print("[green]Offsets sauvegardés dans config.json.[/green]\n")
>>>>>>> REPLACE
```

ÉTAPE 3 — Remplacer la boucle de menu entière (lignes ~206-269).

Remplacer le bloc `while True:` qui affiche le menu print() + input() par questionary.select() :

```
<<<<<<< SEARCH
    while True:
        print('\n=== MENU CAPTURE TEMPLATES ===')
        for i, (name, _) in enumerate(CAPTURES):
            path = os.path.join(TEMPLATES_DIR, f'{name}.png')
            status = 'existe' if os.path.exists(path) else 'manquant'
            print(f'  {i + 1}. [{status:8}] {name}')
        print(f'  {calib_cards_opt}. Calibrer les positions des cartes de skill')
        print(f'  {calib_dice_opt}. Calibrer la position du de')
        print('  0. Quitter')
        print('')

        choice = input('Numero (0 pour quitter) : ').strip()
        if choice == '0':
            print('Termine.')
            break

        if choice == str(calib_cards_opt):
            hwnd = find_mirrorto_window()
            if not hwnd:
                print('Erreur: fenetre MirrorTo perdue. Relance MirrorTo.')
                continue
            calibrate_cards(hwnd)
            continue

        if choice == str(calib_dice_opt):
            hwnd = find_mirrorto_window()
            if not hwnd:
                print('Erreur: fenetre MirrorTo perdue. Relance MirrorTo.')
                continue
            calibrate_dice(hwnd)
            continue

        if not choice.isdigit() or not (1 <= int(choice) <= len(CAPTURES)):
            print(f'Choix invalide.')
            continue

        idx = int(choice) - 1
        name, instruction = CAPTURES[idx]
        path = os.path.join(TEMPLATES_DIR, f'{name}.png')

        if os.path.exists(path):
            confirm = input(f"'{name}.png' existe deja. Ecraser ? [y/N] : ").strip().lower()
            if confirm != 'y':
                print('Annule.')
                continue

        print(f'\n[{name}] {instruction}')
        input('Appuie sur Entree quand le bon ecran est visible dans MirrorTo...')

        hwnd = find_mirrorto_window()
        if not hwnd:
            print('Erreur: fenetre MirrorTo perdue. Relance MirrorTo.')
            continue

        screen = screenshot_window(hwnd)
        if screen is None or screen.size == 0:
            print('Erreur: capture vide. Verifie que MirrorTo est ouvert et non minimise.')
            continue

        saved = show_and_select(screen, name)
        if saved:
            print(f"Template '{name}' sauvegarde avec succes.")
        else:
            print('Capture annulee (aucun rectangle selectionne).')
=======
    while True:
        choices = []
        for name, _ in CAPTURES:
            path = os.path.join(TEMPLATES_DIR, f'{name}.png')
            status = '[green]existe[/green]' if os.path.exists(path) else '[yellow]manquant[/yellow]'
            choices.append(questionary.Choice(
                title=f"{name}  [{('existe' if os.path.exists(path) else 'manquant'):8}]",
                value=name,
            ))
        choices.append(questionary.Choice(title="Calibrer les positions des cartes de skill", value="calib_cards"))
        choices.append(questionary.Choice(title="Calibrer la position du dé", value="calib_dice"))
        choices.append(questionary.Separator())
        choices.append(questionary.Choice(title="Quitter", value="quit"))

        choice = questionary.select(
            "Que veux-tu capturer ou calibrer ?",
            choices=choices,
            pointer="»",
        ).ask()

        if choice is None or choice == "quit":
            console.print("[dim]Terminé.[/dim]")
            break

        if choice == "calib_cards":
            hwnd = find_mirrorto_window()
            if not hwnd:
                console.print("[red]Fenêtre MirrorTo perdue. Relance MirrorTo.[/red]")
                continue
            calibrate_cards(hwnd)
            continue

        if choice == "calib_dice":
            hwnd = find_mirrorto_window()
            if not hwnd:
                console.print("[red]Fenêtre MirrorTo perdue. Relance MirrorTo.[/red]")
                continue
            calibrate_dice(hwnd)
            continue

        idx = next(i for i, (n, _) in enumerate(CAPTURES) if n == choice)
        name, instruction = CAPTURES[idx]
        path = os.path.join(TEMPLATES_DIR, f'{name}.png')

        if os.path.exists(path):
            overwrite = questionary.confirm(
                f"'{name}.png' existe déjà. Écraser ?",
                default=False,
            ).ask()
            if not overwrite:
                console.print("[yellow]Annulé.[/yellow]")
                continue

        console.print(Panel(
            f"{instruction}\n[dim]Appuie sur Entrée quand le bon écran est visible dans MirrorTo...[/dim]",
            title=f"[bold]{name}[/bold]",
            border_style="blue",
        ))
        input()

        hwnd = find_mirrorto_window()
        if not hwnd:
            console.print("[red]Fenêtre MirrorTo perdue. Relance MirrorTo.[/red]")
            continue

        screen = screenshot_window(hwnd)
        if screen is None or screen.size == 0:
            console.print("[red]Erreur : capture vide. Vérifie que MirrorTo est ouvert et non minimisé.[/red]")
            continue

        saved = show_and_select(screen, name)
        if saved:
            console.print(f"[green]Template '{name}' sauvegardé avec succès.[/green]")
        else:
            console.print("[yellow]Capture annulée (aucun rectangle sélectionné).[/yellow]")
>>>>>>> REPLACE
```

ÉTAPE 4 — Supprimer les variables `calib_cards_opt` et `calib_dice_opt` devenues inutiles :
```
<<<<<<< SEARCH
    calib_cards_opt = len(CAPTURES) + 1
    calib_dice_opt  = len(CAPTURES) + 2

    while True:
=======
    while True:
>>>>>>> REPLACE
```
</action>
<acceptance_criteria>
- `grep -n "questionary.select" capture_templates.py` retourne 1 résultat (dans main())
- `grep -n "questionary.confirm" capture_templates.py` retourne 1 résultat (overwrite)
- `grep -n "input('Numero" capture_templates.py` retourne 0 résultats (ancien menu supprimé)
- `grep -n "calib_cards_opt\|calib_dice_opt" capture_templates.py` retourne 0 résultats
- `grep -n "print('Erreur: fenetre" capture_templates.py` retourne 0 résultats
- `grep -n "Fenêtre MirrorTo perdue" capture_templates.py` retourne 2 résultats (calib_cards + calib_dice)
- `grep -n "if __name__" capture_templates.py` retourne 1 résultat (main() toujours présent)
- `grep -c "def main" capture_templates.py` retourne 1 (fonction main() intacte)
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `grep -n "import questionary\|from rich" capture_templates.py` → 3 résultats
2. `grep -n "console = Console()" capture_templates.py` → 1 résultat
3. `grep -n "Panel(" capture_templates.py` → au moins 4 résultats (3 calibration + 1 instruction template)
4. `grep -n "questionary.select\|questionary.confirm" capture_templates.py` → 2 résultats
5. `grep -n "input('Numero\|print('\\\\n===" capture_templates.py` → 0 résultats
6. `python -c "import capture_templates"` → s'exécute sans erreur
7. Lecture manuelle capture_templates.py lignes 1-20 pour confirmer imports corrects
</verification>

<must_haves>
- Menu navigation flèches via questionary.select() — cohérent avec main.py (UX-07)
- Instructions de calibration dans des rich Panel avec titre et bordure cyan (UX-06)
- Tous les messages de statut via console.print() avec couleurs (UX-06, UX-07)
- La logique de capture OpenCV, calibrate_offset, calibrate_cards, calibrate_dice est inchangée
- `def main()` et `if __name__ == "__main__":` toujours présents
- Aucun print() utilisateur restant (seulement console.print())
</must_haves>
