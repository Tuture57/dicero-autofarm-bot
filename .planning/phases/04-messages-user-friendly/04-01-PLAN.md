---
id: "04-01"
wave: 1
title: "Refactor bot.py — RichHandler + verbosité épurée + messages instructifs"
goal: "Remplacer logging.basicConfig par RichHandler, passer les clics en DEBUG, réécrire les messages d'erreur en français instructif"
requirements_addressed: [UX-05, UX-07]
files_modified:
  - bot.py
autonomous: true
---

<objective>
Moderniser les messages de bot.py sans toucher à la logique de capture, détection ou clics. Trois changements :
1. Remplacer `logging.basicConfig(...)` par `RichHandler` de `rich.logging` — timestamps activés, couleurs par niveau
2. Passer tous les `log.info` de clics répétitifs en `log.debug` (masqués par défaut) — garder INFO pour les changements d'état
3. Réécrire les messages d'erreur en français clair avec action à faire, et ajouter une variable `last_state` pour loguer uniquement les changements d'état
</objective>

<tasks>
<task id="T1">
<title>Remplacer logging.basicConfig par RichHandler</title>
<read_first>
- bot.py (lire lignes 1-20 pour voir les imports et la config logging actuelle)
</read_first>
<action>
ÉTAPE 1 — Ajouter l'import RichHandler après `import logging` :

SEARCH/REPLACE :
```
<<<<<<< SEARCH
import logging
import win32gui
=======
import logging
from rich.logging import RichHandler
import win32gui
>>>>>>> REPLACE
```

ÉTAPE 2 — Remplacer la configuration logging :

SEARCH/REPLACE :
```
<<<<<<< SEARCH
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(message)s')
log = logging.getLogger(__name__)
=======
logging.basicConfig(
    level=logging.INFO,
    format="%(message)s",
    datefmt="[%H:%M:%S]",
    handlers=[RichHandler(rich_tracebacks=True)],
)
log = logging.getLogger(__name__)
>>>>>>> REPLACE
```

Résultat attendu : les messages affichent l'heure [HH:MM:SS] en couleur selon le niveau (INFO=vert, WARNING=jaune, ERROR=rouge), via RichHandler.
</action>
<acceptance_criteria>
- `grep -n "RichHandler" bot.py` retourne 2 résultats (import + handlers=[...])
- `grep -n "from rich.logging" bot.py` retourne 1 résultat
- `grep -n "logging.basicConfig" bot.py` retourne 1 résultat (la version remplacée avec handlers=[RichHandler])
- `grep -n "'%(asctime)s" bot.py` retourne 0 résultats (ancien format supprimé)
</acceptance_criteria>
</task>

<task id="T2">
<title>Passer les clics répétitifs en log.debug et ajouter last_state</title>
<read_first>
- bot.py (lire lignes 210-415 pour voir tous les log.info de clics et la boucle principale)
</read_first>
<action>
ÉTAPE 1 — Passer les clics individuels répétitifs en log.debug :

handle_dungeon (ligne ~214) :
```
<<<<<<< SEARCH
        log.info("Clicking Attack")
=======
        log.debug("Clicking Attack")
>>>>>>> REPLACE
```

handle_wheel (ligne ~223) :
```
<<<<<<< SEARCH
        log.info("Clicking Try Your Luck")
=======
        log.debug("Clicking Try Your Luck")
>>>>>>> REPLACE
```

handle_select_skill (ligne ~241) :
```
<<<<<<< SEARCH
    log.info(f"Selecting skill card at {chosen}")
=======
    log.debug(f"Selecting skill card at {chosen}")
>>>>>>> REPLACE
```

handle_select_dice (ligne ~251) :
```
<<<<<<< SEARCH
    log.info(f"Selecting die at {pos}")
=======
    log.debug(f"Selecting die at {pos}")
>>>>>>> REPLACE
```

handle_chest (ligne ~259) :
```
<<<<<<< SEARCH
        log.info("Clicking chest button")
=======
        log.debug("Clicking chest button")
>>>>>>> REPLACE
```

handle_edit_btn (ligne ~268) :
```
<<<<<<< SEARCH
        log.info("Clicking Edit button")
=======
        log.debug("Clicking Edit button")
>>>>>>> REPLACE
```

handle_menu (ligne ~277) :
```
<<<<<<< SEARCH
        log.info("Clicking New Game on menu")
=======
        log.debug("Clicking New Game on menu")
>>>>>>> REPLACE
```

handle_rewards (ligne ~286) :
```
<<<<<<< SEARCH
        log.info("Clicking rewards button")
=======
        log.debug("Clicking rewards button")
>>>>>>> REPLACE
```

handle_confirm_popup (ligne ~296) :
```
<<<<<<< SEARCH
        log.info("Clicking Confirm on popup")
=======
        log.debug("Clicking Confirm on popup")
>>>>>>> REPLACE
```

ÉTAPE 2 — Ajouter `last_state` avant la boucle principale et loguer uniquement les changements d'état :

```
<<<<<<< SEARCH
    consecutive_unknown = 0

    while not _stop_event.is_set():
=======
    consecutive_unknown = 0
    last_state = None

    while not _stop_event.is_set():
>>>>>>> REPLACE
```

Remplacer le log d'état dans la boucle (qui loguait chaque cycle) :
```
<<<<<<< SEARCH
            consecutive_unknown = 0
            log.info(f"State: {state}")
=======
            consecutive_unknown = 0
            if state != last_state:
                log.info(f"État : {state}")
                last_state = state
>>>>>>> REPLACE
```
</action>
<acceptance_criteria>
- `grep -n "log.info(\"Clicking" bot.py` retourne 0 résultats
- `grep -n "log.debug(\"Clicking" bot.py` retourne au moins 5 résultats
- `grep -n "last_state" bot.py` retourne au moins 3 résultats (initialisation + comparaison + affectation)
- `grep -n "log.info(f\"State:" bot.py` retourne 0 résultats (ancien log supprimé)
- `grep -n "log.info(f\"État" bot.py` retourne 1 résultat (nouveau log conditionnel)
</acceptance_criteria>
</task>

<task id="T3">
<title>Réécrire les messages d'erreur et de statut en français instructif</title>
<read_first>
- bot.py (lire lignes 40-60 et 300-420 pour voir tous les messages restants)
</read_first>
<action>
Réécrire les messages INFO/WARNING/ERROR notables en français clair :

Echap détecté (ligne ~46) :
```
<<<<<<< SEARCH
                log.info("Echap détecté — arrêt du bot...")
=======
                log.info("Echap détecté — arrêt en cours...")
>>>>>>> REPLACE
```

Bot arrêté (ligne ~57) :
```
<<<<<<< SEARCH
    log.info("Bot arrêté proprement.")
=======
    log.info("Bot arrêté.")
>>>>>>> REPLACE
```

Fenêtre introuvable au démarrage (ligne ~308) :
```
<<<<<<< SEARCH
        log.error(f"MirrorTo window not found. Make sure the app is open and its title contains '{MIRRORTO_TITLE}'.")
=======
        log.error(f"Fenêtre MirrorTo introuvable. Lance MirrorTo, puis réessaie.")
>>>>>>> REPLACE
```

Fenêtre trouvée (ligne ~314) — passer en debug car technique :
```
<<<<<<< SEARCH
    log.info(f"Found MirrorTo window: hwnd={hwnd}, size={ref_w}x{ref_h} rect={rect}")
=======
    log.debug(f"Fenêtre MirrorTo trouvée : hwnd={hwnd}, taille={ref_w}x{ref_h}")
>>>>>>> REPLACE
```

MirrorTo au premier plan (ligne ~324) :
```
<<<<<<< SEARCH
    log.info("MirrorTo mis au premier plan — Echap pour arrêter")
=======
    log.info("Bot démarré — appuie sur Echap pour arrêter.")
>>>>>>> REPLACE
```

Template manquant (ligne ~345) :
```
<<<<<<< SEARCH
            log.warning(f"Template not found: {name}.png - will use fallback logic")
=======
            log.warning(f"Template manquant : {name}.png — recalibre les templates depuis le menu.")
>>>>>>> REPLACE
```

Templates chargés (ligne ~347) :
```
<<<<<<< SEARCH
    log.info(f"Loaded {sum(1 for v in templates.values() if v is not None)}/{len(templates_to_load)} templates")
    log.info("Bot démarré. Appuie sur Echap pour arrêter.")
=======
    log.info(f"Templates chargés : {sum(1 for v in templates.values() if v is not None)}/{len(templates_to_load)}")
>>>>>>> REPLACE
```

Fenêtre perdue pendant le run (ligne ~359) :
```
<<<<<<< SEARCH
                log.warning("MirrorTo window lost, retrying...")
=======
                log.warning("Fenêtre MirrorTo perdue — nouvelle tentative...")
>>>>>>> REPLACE
```

Taille changée (ligne ~367) :
```
<<<<<<< SEARCH
                log.warning(f"TAILLE CHANGEE: actuelle={cur_w}x{cur_h}, reference={ref_w}x{ref_h} — templates peuvent ne plus matcher!")
=======
                log.warning(f"Taille de fenêtre changée ({cur_w}x{cur_h} au lieu de {ref_w}x{ref_h}) — les templates peuvent ne plus fonctionner.")
>>>>>>> REPLACE
```

Fenêtre trop petite (ligne ~371) :
```
<<<<<<< SEARCH
                log.warning("Fenetre trop petite ou minimisee, attente...")
=======
                log.warning("Fenêtre trop petite ou minimisée — en attente...")
>>>>>>> REPLACE
```

Unknown state (ligne ~379) :
```
<<<<<<< SEARCH
                    log.warning(f"Unknown state for {consecutive_unknown} cycles")
=======
                    log.warning(f"État inconnu depuis {consecutive_unknown} cycles — vérification en cours...")
>>>>>>> REPLACE
```

Config card non calibrée (ligne ~238) :
```
<<<<<<< SEARCH
        log.error("card_positions non calibre dans config.json — lance capture_templates.py pour calibrer")
=======
        log.error("Positions des cartes non calibrées — lance 'Calibrer les templates' depuis le menu.")
>>>>>>> REPLACE
```

Config dice non calibrée (ligne ~249) :
```
<<<<<<< SEARCH
        log.error("dice_position non calibre dans config.json — lance capture_templates.py pour calibrer")
=======
        log.error("Position du dé non calibrée — lance 'Calibrer les templates' depuis le menu.")
>>>>>>> REPLACE
```

Erreur générique (ligne ~410) :
```
<<<<<<< SEARCH
            log.error(f"Error: {e}")
=======
            log.error(f"Erreur inattendue : {e}")
>>>>>>> REPLACE
```
</action>
<acceptance_criteria>
- `grep -n "window not found" bot.py` retourne 0 résultats
- `grep -n "MirrorTo introuvable" bot.py` retourne 1 résultat
- `grep -n "non calibre" bot.py` retourne 0 résultats
- `grep -n "non calibrée" bot.py` retourne 2 résultats (cards + dice)
- `grep -n "TAILLE CHANGEE" bot.py` retourne 0 résultats
- `grep -n "Taille de fenêtre" bot.py` retourne 1 résultat
- `grep -n "Error:" bot.py` retourne 0 résultats (message anglais supprimé)
- `grep -n "Erreur inattendue" bot.py` retourne 1 résultat
- `grep -n "Template manquant" bot.py` retourne 1 résultat
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `grep -n "RichHandler" bot.py` → 2 résultats
2. `grep -n "log.info(\"Clicking" bot.py` → 0 résultats
3. `grep -n "last_state" bot.py` → 3+ résultats
4. `grep -n "window not found\|non calibre\|TAILLE CHANGEE\|Error:" bot.py` → 0 résultats
5. `python -c "import bot"` → s'exécute sans erreur (pas de popup UAC, peut échouer sur win32 sans MirrorTo)
6. Lecture manuelle bot.py lignes 1-20 pour confirmer RichHandler bien configuré
</verification>

<must_haves>
- RichHandler remplace logging.basicConfig (UX-07 : cohérence visuelle rich)
- Clics individuels passés en log.debug — seuls les changements d'état sont en INFO (UX-05 : messages clairs)
- Tous les messages d'erreur en français instructif avec action à faire (UX-05)
- Aucun message en anglais dans les logs utilisateur
- La logique de capture, détection et clics est inchangée
</must_haves>
